pipeline {
    agent {
        docker { image 'puppet/puppet-agent' }
    }
    stages {
        stage('Test') {
            steps {
                sh 'puppet --version'
            }
        }
    }
}
